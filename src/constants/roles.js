/**
 * Application Roles for Role-Based Access Control (RBAC)
 * Supported roles: admin, customer, technician
 */
export const ROLES = {
    CUSTOMER: "customer",
    ADMIN: "admin",
    TECHNICIAN: "technician",
};

export const ALL_ROLES = Object.values(ROLES);

